
/*
    Corey Russ
    Lab 3
    CIS 22c
    QueueTest.cpp
*/

/*
#include <iostream>
#include "Queue.h"
using namespace std;

int main()
{
    Queue q;

    cout << "\nThis should print front\n";
    cout << q.get_front();

    cout << "\nThis should print 0\n";
    cout << q.get_size();

    cout << "\nAdding Sally to the queue\n";
    q.enqueue("Sally");
    cout << q.get_front();

    cout << "\nThis should print Sally\n";
    cout << q.get_front();

    cout << "\nAdding George to the queue\n";
    q.enqueue("George");

    cout << "\nThis should print Sally\n";
    cout << q.get_front();

    cout << "\nDequeuing the first item...\n";
    q.dequeue();

    cout << "\nThe front should be George\n";
    cout << q.get_front();

    cout << "\nThe size should be 1\n";
    cout << q.get_size();

    cout << "\nAdding Sam to the queue\n";
    q.enqueue("Sam");

    cout << "\nAdding Holly to the queue\n";
    q.enqueue("Holly");

    Queue q_copy(q);

    cout << "\nThe original queue is: ";
    q.print();

    cout << "\nThe copied queue is: ";
    q.print();

    q.dequeue();
    q.dequeue();
    cout << "One item should print: ";
    q.print();


    q.dequeue();
    cout << "Nothing should print: ";
    q.print();

    return 0;
}
*/
